import 'package:flutter/material.dart';

/// Paleta fixa da marca (independente do tema claro/escuro).
class BrandColors {
  const BrandColors._();

  /// Verde de destaque fixo para ações primárias (compose, FAB, etc).
  static const Color accentGreen = Color(0xFF00C853); // Material Green A700
}









